# pyproject.toml
[project]
name = "pm-ontology-backend"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    # ── 웹 프레임워크 ──────────────────────────────────────
    "fastapi>=0.111",           # 비동기 REST API, 자동 OpenAPI 문서
    "uvicorn[standard]>=0.29",  # ASGI 서버
    "python-multipart>=0.0.9",  # 폼 데이터 파싱

    # ── DB: PostgreSQL ─────────────────────────────────────
    "sqlalchemy>=2.0",          # ORM (async 지원)
    "asyncpg>=0.29",            # 비동기 PostgreSQL 드라이버
    "alembic>=1.13",            # DB 마이그레이션

    # ── DB: Triple Store (Fuseki) ──────────────────────────
    "SPARQLWrapper>=2.0",       # SPARQL HTTP 클라이언트
    "rdflib>=7.0",              # RDF 파싱 / 트리플 조작

    # ── 비동기 워커 ────────────────────────────────────────
    "celery>=5.4",              # 비동기 태스크 큐
    "redis>=5.0",               # Celery 브로커 + 결과 백엔드
    "flower>=2.0",              # Celery 모니터링 UI (개발용)

    # ── LLM (Semantic Interpreter) ─────────────────────────
    "anthropic>=0.26",          # Claude API (Signal 분류)
    # "openai>=1.30",           # 대안: GPT-4o 사용 시

    # ── 알림 발송 ──────────────────────────────────────────
    "slack-sdk>=3.27",          # Slack 메시지 발송 (Bolt SDK)
    # "sendgrid>=6.11",         # 이메일 알림 (Phase 2)

    # ── 설정 · 검증 ────────────────────────────────────────
    "pydantic>=2.7",            # 데이터 검증 + DTO
    "pydantic-settings>=2.3",   # 환경변수 관리 (.env)
    "python-dotenv>=1.0",       # .env 파일 로드

    # ── HTTP 클라이언트 ────────────────────────────────────
    "httpx>=0.27",              # 외부 API 호출 (비동기)

    # ── 보안 ───────────────────────────────────────────────
    "python-jose[cryptography]>=3.3",  # JWT 인증
    "passlib[bcrypt]>=1.7",            # 비밀번호 해싱
    "cryptography>=42",                # HMAC webhook 서명 검증
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",              # TestClient용
    "pytest-cov>=5.0",
    "ruff>=0.4",                # 린터 + 포매터 (black 대체)
    "mypy>=1.10",               # 타입 체커
]

# ── 빌드 ────────────────────────────────────────────────────
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
line-length = 100
select = ["E", "F", "I", "UP"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
